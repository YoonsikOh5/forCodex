{
  "name": "cardia-online-duel",
  "version": "0.1.0",
  "private": true,
  "description": "Korean two-player web prototype for a Cardia-style duel.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
